---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "common"
    paths:
      - 'databases/postgresql.yaml'
      - 'databases/mysql.yaml'
      - 'client.yaml'
      - 'common.yaml'
